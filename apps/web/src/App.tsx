export function App() {
  return (
    <main className="shell">
      <section className="hero">
        <p className="eyebrow">LifeOS</p>
        <h1>Typed state, local privacy boundary, real-time sync.</h1>
        <p className="copy">
          Foundation scaffolding is in place. The next build steps are auth, local bridge pairing,
          signed ingest, and the integration event pipeline.
        </p>
      </section>
    </main>
  );
}
